---
tags: [frontend, react, ssr]
---
# [[React]] Server Components (RSC)
Paradigma que permite a los componentes de [[React]] ejecutarse y renderizarse exclusivamente en el servidor, enviando un stream JSON binario al cliente sin incluir dependencias pesadas en el bundle JavaScript. Se comunica con APIs en [[FastAPI]] o [[NestJS]].
