---
tags: [frontend, react, performance]
---
# Virtual DOM
Representación ligera en memoria del DOM real del navegador. [[React]] compara el Virtual DOM actual con el nuevo mediante el algoritmo de reconciliación de [[React Fiber Architecture]] para aplicar mutaciones mínimas al DOM real.
